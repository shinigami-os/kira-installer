set timeout=3
set default=0

menuentry "Kira Linux Installer" {
    insmod search
    insmod search_label
    search --no-floppy --label --set=root KIRA
    linux /boot/bzImage rdinit=/sbin/runit-init console=tty1
    initrd /boot/initramfs.cpio.gz
}